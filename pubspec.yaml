name: prompteur_video
description: Application Prompteur Vidéo pour Android
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  provider: ^6.1.2
  permission_handler: ^11.3.1
  wakelock_plus: ^1.2.8
  gal: ^1.1.0

dependency_overrides:
  package_info_plus: '>=8.0.0 <9.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
